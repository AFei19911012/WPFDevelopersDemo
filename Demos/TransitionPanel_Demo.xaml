<UserControl x:Class="WPFDevelopersDemo.Demos.TransitionPanel_Demo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="600"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Canvas ClipToBounds="True" x:Name="PART_Canvas" VerticalAlignment="Top" HorizontalAlignment="Left" Width="800">
            <Canvas.Background>
                <ImageBrush ImageSource="../Resource/Image/Craouse/0.jpg"/>
            </Canvas.Background>
        </Canvas>
        <Button Grid.Row="1" x:Name="btnContent" Content="下一步" Margin="0,-50,0,0" Click="Button_Click" Style="{StaticResource PrimaryButton}" Width="80"/>
    </Grid>
</UserControl>